import { Plate } from "@/app/cardapio/page";

export default function MenuCard( { nome, descricao, categoria, preco, imagem, destacado, fixo, criado_em, atualizado_em } : Plate) {
    return (
        <article>

        </article>
    )
}